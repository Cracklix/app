
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // DEV MODE: Full open access to unblock development and data seeding.
    // This ensures all features (mocks, sessions, analysis, etc.) work without permission errors.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
