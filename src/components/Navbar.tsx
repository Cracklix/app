/**
 * @fileOverview Neutralized redundant root component to prevent module resolution conflicts.
 * The active high-fidelity version is located in src/components/layout/Navbar.tsx.
 */
export default function RedundantNavbar() {
  return null;
}
