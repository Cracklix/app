'use client';

/**
 * @fileOverview Neutralized component as Stats are now integrated directly into the Hero Hub 
 * to match the high-fidelity reference design.
 */
export default function StatsBar() {
  return null;
}
